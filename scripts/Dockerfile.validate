FROM python:3.12-slim

RUN pip install --no-cache-dir jinja2 pyyaml

WORKDIR /blueprints

COPY validate_blueprints.py /opt/validate_blueprints.py

ENTRYPOINT ["python", "/opt/validate_blueprints.py"]
